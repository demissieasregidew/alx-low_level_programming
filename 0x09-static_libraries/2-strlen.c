#include "main.h"
#include <string.h>
/**
 * _strlen - calculate the length of a string.
 *
 * @s: the string to calculate it's length. y
 * Return: length of a string.
 */

int _strlen(char *s)
{
return (strlen(s));
}
